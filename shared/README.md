# Shared Libraries

This directory is reserved for shared code used across multiple agents.

Currently, each agent copies shared modules (models, repositories, services) locally.
Future refactoring may extract common code into a shared Python package here.
